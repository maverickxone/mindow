[package]
name = "mindow-core"
version = "0.9.3"
edition = "2021"
description = "Core library for Mindow: collector, filter, rule engine, and data types"

[dependencies]
sysinfo = "0.33"
battery = "0.7"

[dev-dependencies]
proptest = "1"
